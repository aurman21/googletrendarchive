# googletrendarchive
GitHub repo companion to the GoogleTrendArchive dataset. Contains the README, data loader demos (Python), the preprocessing script (R) and the original script used for scraping/dataset collection (Python). Data files are hosted on Hugging Face due to size.
